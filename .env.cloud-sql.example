# Cloud SQL Configuration Example with IAM Authentication
# Copy this file to .env and configure your Cloud SQL settings

# Cloud SQL Configuration
USE_CLOUD_SQL=true
CLOUD_SQL_INSTANCE_CONNECTION_NAME=your-project-id:your-region:your-instance-name

# IAM Database Authentication (Recommended for production)
USE_IAM_AUTH=true
CLOUD_SQL_IAM_USER=your-service-account@your-project.iam.gserviceaccount.com

# Database Configuration
DB_NAME=your_database_name
# DB_USER and DB_PASSWORD not needed with IAM auth for Cloud SQL
# These are only used for local MariaDB development

# Google Cloud Configuration
GCP_PROJECT_ID=your-project-id
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account.json

# AI Services
GOOGLE_API_KEY=your_google_api_key
USE_VERTEX_AI=false
VERTEX_AI_PROJECT_ID=your-project-id
VERTEX_AI_LOCATION=us-central1

# App Configuration
ENVIRONMENT=development
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# MySQL Root Password (not used with Cloud SQL)
MYSQL_ROOT_PASSWORD=root_password